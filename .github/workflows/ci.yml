name: CI

on:
  - push
  - pull_request

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        napalm_version:
          - "3.*"
          - "4.*"
        python_version:
          - "3.6"
          - "3.7"
          - "3.8"
        exclude:
          - napalm_version: "4.*"
            python_version: "3.6"
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Set up Python ${{ matrix.python_version }}
        id: setup-python
        uses: actions/setup-python@v2
        with:
          python-version: ${{ matrix.python_version }}

      - name: Install pipenv
        run: pip install pipenv

      - uses: actions/cache@v2
        with:
          path: ~/.local/share/virtualenvs
          key: ${{ runner.os }}-python-${{ matrix.python_version }}-${{ matrix.napalm_version }}-pipenv-${{ hashFiles('Pipfile.lock') }}

      - name: Install requirements
        run: pipenv --python python3 install --dev

      - name: Install napalm
        run: pipenv install napalm==${{ matrix.napalm_version }}

      - name: Lint
        run: pipenv run pylint napalm_ros

      - name: Format
        run: >
          pipenv run yapf -dr
          napalm_ros
          tests

      - name: Unit tests
        run: pipenv run pytest tests

      - name: Package
        run: pipenv run python setup.py sdist

      - name: Publish package
        if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags') && matrix.python_version == '3.7' && matrix.napalm_version == '4.*'
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          user: __token__
          password: ${{ secrets.pypi_password }}
